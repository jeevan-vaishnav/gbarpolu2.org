// To run the Next.js project on mobile later, first start the development server
// using`npm run dev -- --hostname 0.0.0.0`.After the server starts, open a new terminal and
// run`npx localtunnel --port 3000`.It will generate a public URL like`https://example.loca.lt`.
// Open that URL on your mobile browser while keeping both terminals running.
// If LocalTunnel asks for IP verification, enter the public IP shown on the page and continue.
//     Any code changes saved in the project will automatically reflect on the mobile browser with live reload.
